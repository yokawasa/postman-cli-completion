#compdef postman
compdef _postman postman
# Zsh completion for Postman CLI (postman v1.41.0)
# Auto-generated by scripts/generate.mjs — DO NOT EDIT
# Source: spec/commands.json
# https://github.com/yokawasa/postman-cli-completion

_postman() {
  local context state state_descr line
  typeset -A opt_args

  _arguments -C \
    '(-v --version)'{-v,--version}'[output the version number]' \
    '--silent[Silences all terminal output]' \
    '--color[Enable/Disable colored output (auto|on|off) (default\: "auto")]:value:(auto on off)' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_commands' \
    '*::arg:->args'

  case $state in
    args)
      case $words[1] in
        login) _postman_login ;;
        logout) _postman_logout ;;
        collection) _postman_collection ;;
        api) _postman_api ;;
        runner) _postman_runner ;;
        spec) _postman_spec ;;
        monitor) _postman_monitor ;;
        workspace) _postman_workspace ;;
        performance) _postman_performance ;;
        flows|fl) _postman_flows ;;
        request) _postman_request ;;
        sdk) _postman_sdk ;;
        mock) _postman_mock ;;
        application|app) _postman_application ;;
        simulate) _postman_simulate ;;
        context) _postman_context ;;
        search) _postman_search ;;
        webhook) _postman_webhook ;;
        dataset) _postman_dataset ;;
      esac
      ;;
  esac
}

_postman_commands() {
  local -a commands
  commands=(
    'login:Authenticate with Postman to access your resources.'
    'logout:Delete the stored Postman API key.'
    'collection:Run and test your Postman collections directly from the command line.'
    'api:Publish and test your APIs directly from the command line'
    'runner:Run runners on your own environments for monitoring your APIs'
    'spec:Lint and validate Specifications from the command line'
    'monitor:Invoke a monitor run and display results'
    'workspace:Manage workspace resources.'
    'performance:Manage performance tests on your collections.'
    'flows:Manage and interact with flows.'
    'fl:Alias for flows'
    'request:Send HTTP requests from the command line with full Postman capabilities including authentication, environment variables, assertions, retries, and detailed output control.'
    'sdk:Generate and manage your SDKs directly from the command line.'
    'mock:Run and manage local Postman mock servers.'
    'application:Application-level commands\: initialise config and run tests.'
    'app:Alias for application'
    'simulate:Start mock servers with simulated failure scenarios.'
    'context:\[Beta\] Get API context for AI coding agents directly from the command line.'
    'search:Search for Postman element types (requests, collections, workspaces, and more).'
    'webhook:Create and manage Postman webhooks.'
    'dataset:\[BETA\] Work with Postman datasets defined in local YAML files.'
  )
  _describe -t commands 'postman command' commands
}

_postman_login() {
  _arguments \
    '--alias[Specify the alias of the API key to fetch your resources using it. (This option is discontinued and will be removed in a future release.)]:alias' \
    '--with-api-key[Specify the API key directly.]:apikey' \
    '--region[Specify the region for authentication. Use "eu" for EU region.]:region:(us eu)' \
    '--verbose[Show detailed error information]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_logout() {
  _arguments \
    '--alias[Specify the alias of the API key to fetch your resources using it. (This option is discontinued and will be removed in a future release.)]:alias' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_collection() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_collection_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        migrate) _postman_collection_migrate ;;
        lint) _postman_collection_lint ;;
        ai-readiness) _postman_collection_ai_readiness ;;
        run) _postman_collection_run ;;
      esac
      ;;
  esac
}

_postman_collection_commands() {
  local -a sub
  sub=(
    'migrate:Migrate a v2.1 collection to the v3 format'
    'lint:Run linting on a local v3 collection at the given file or directory path.'
    'ai-readiness:Score a Postman collection for AI readiness by ID, local file path, or local-mode directory.'
    'run:Initiate a Postman collection run from a given ID or path.'
  )
  _describe -t commands 'collection command' sub
}

_postman_collection_migrate() {
  _arguments \
    '(-o --output)'{-o,--output}'[Output directory (default\: ./<collection-name>)]:dir:_files -g "*.json"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_collection_lint() {
  _arguments \
    '(-f --fail-severity)'{-f,--fail-severity}'[Exit with failure if diagnostics at this level or above are present. \[choices\: "error", "warning"\] (default\: "error")]:value' \
    '(-r --reporter)'{-r,--reporter}'[Output format\: cli (human-readable) or json. (default\: "cli")]:value' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_collection_ai_readiness() {
  _arguments \
    '(-o --output)'{-o,--output}'[Output format for the results. \[choices\: "cli", "json", "html"\]]:value' \
    '--min-score[Exit with a non-zero code if the overall score is below this threshold (0-100).]:n' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:collectionId/Path:_files -g "*.json"'
}

_postman_collection_run() {
  _arguments \
    '(-e --environment)'{-e,--environment}'[Specify an Id or path to a Postman Environment]:path:_files -g "*.json"' \
    '(-g --globals)'{-g,--globals}'[Specify an Id or path to a file containing Postman Globals]:path:_files -g "*.json"' \
    '(-r --reporters)'{-r,--reporters}'[Specify the reporters to use for the run\: cli,json,junit,html. For multiple reporters, provide their names as a comma-separated list (E.g., -r cli,json) (default\: \["cli"\])]:reporter:(cli json junit html)' \
    '--reporter-json-structure[\[Optional\] Generate JSON report using the Newman schema. By default, JSON reports use the native structure of Postman CLI.]' \
    '(-n --iteration-count)'{-n,--iteration-count}'[Define the number of iterations to run]:n' \
    '(-d --iteration-data)'{-d,--iteration-data}'[Specify a data file to use for iterations (either JSON or CSV)]:path:_files -g "*.(json|csv)"' \
    '--iteration-data-dataset[\[BETA\] Path to a .dataset.yaml whose view drives the iteration data. Mutually exclusive with --iteration-data. Requires --iteration-data-view.]:path:_files -g "*.(json|csv)"' \
    '--iteration-data-view[\[BETA\] Name or id of the view to execute (within --iteration-data-dataset). The view'\''s result set drives one iteration per row.]:nameOrId' \
    '--dataset[\[BETA\] Pass --dataset multiple times to load several .dataset.yaml files (or directories containing them). Each is made available to scripts as pm.datasets(<id>). When provided, auto-discovery of .dataset.yaml files from the collection'\''s parent repo is disabled. (default\: \[\])]:pathOrDir:_files -g "*.json"' \
    '-i[Specify the request/folder id, name, or path to run from the collection. Use a path (e.g. "FolderName/RequestName") to disambiguate items with the same name. Can be specified multiple times to run multiple items (default\: \[\])]:id' \
    '--global-var[Allows the specification of global variables via the command line, in a key=value format]:value' \
    '--env-var[Allows the specification of environment variables via the command line, in a key=value format (default\: \[\])]:value' \
    '--integration-id[Specify the integration id for the report. (Only supported in the US region)]:integration-id' \
    '--postman-api-key[API Key used to load the resources from the Postman API (Only supported in the US region, use '\''postman login --region'\'' to authenticate instead)]:apiKey' \
    '--alias[Specify the alias of the API key to fetch remote resources using it.]:alias' \
    '--bail[Specify whether or not to gracefully stop a collection run on encountering an error and whether to end the run with an error based on the optional modifier]:modifiers:(folder failure)' \
    '--ignore-redirects[Prevents Postman CLI from automatically following 3XX redirect responses]' \
    '(-x --suppress-exit-code)'{-x,--suppress-exit-code}'[Specify whether or not to override the default exit code for the current run]' \
    '--silent[Prevents Postman CLI from showing output to CLI]' \
    '--disable-unicode[Forces Unicode compliant symbols to be replaced by their plain text equivalents]' \
    '--delay-request[Specify the extent of delay between requests (milliseconds) (default\: 0)]:n' \
    '--timeout[Specify a timeout for collection run (milliseconds) (default\: 0)]:n' \
    '--timeout-request[Specify a timeout for requests (milliseconds) (default\: 0)]:n' \
    '--timeout-script[Specify a timeout for scripts (milliseconds) (default\: 0)]:n' \
    '--working-dir[Specify the path to the working directory]:path:_files -/' \
    '--no-insecure-file-read[Prevents reading the files situated outside of the working directory]' \
    '(-k --insecure)'{-k,--insecure}'[Disables SSL validations]' \
    '--ssl-client-cert-list[Specify the path to a client certificates configurations (JSON)]:path:_files -g "*.(pem|crt|key)"' \
    '--ssl-client-cert[Specify the path to a client certificate (PEM)]:path:_files -g "*.(pem|crt|key)"' \
    '--ssl-client-key[Specify the path to a client certificate private key]:path:_files -g "*.(pem|crt|key)"' \
    '--ssl-client-passphrase[Specify the client certificate passphrase (for protected key)]:passphrase' \
    '--ssl-extra-ca-certs[Specify additionally trusted CA certificates (PEM)]:path:_files -g "*.(pem|crt|key)"' \
    '--cookie-jar[Specify the path to a custom cookie jar (serialized tough-cookie JSON)]:path:_files -g "*.json"' \
    '--export-cookie-jar[Exports the cookie jar to a file after completing the run]:path:_files -g "*.json"' \
    '--verbose[Show detailed information of collection run and each request sent]' \
    '--mock[Start a mock server from manifest (.json) file]:path:_files -g "*.json"' \
    '--simulate[Start mock servers with fault-injection scenarios from a .sim.yaml file]:path:_files -g "*.(sim.yaml|yaml|yml)"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '--reporter-json-export[Path to save the report for the given reporter.]:value:_files -g "*.(json|xml|html)"' \
    '--reporter-junit-export[Path to save the report for the given reporter.]:value:_files -g "*.(json|xml|html)"' \
    '--reporter-html-export[Path to save the report for the given reporter.]:value:_files -g "*.(json|xml|html)"' \
    '--reporter-json-omitRequestBodies[Remove all request bodies from the report.]' \
    '--reporter-html-omitRequestBodies[Remove all request bodies from the report.]' \
    '--reporter-json-omitResponseBodies[Remove all response bodies from the report.]' \
    '--reporter-html-omitResponseBodies[Remove all response bodies from the report.]' \
    '--reporter-json-omitHeaders[Remove all request and response headers from the report.]' \
    '--reporter-html-omitHeaders[Remove all request and response headers from the report.]' \
    '--reporter-json-omitAllHeadersAndBody[Remove all request/response headers and bodies from the report.]' \
    '--reporter-html-omitAllHeadersAndBody[Remove all request/response headers and bodies from the report.]' \
    '*:collectionId/Path:_files -g "*.json"'
}

_postman_api() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_api_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        lint) _postman_api_lint ;;
        publish) _postman_api_publish ;;
      esac
      ;;
  esac
}

_postman_api_commands() {
  local -a sub
  sub=(
    'lint:Run linting on the schema of the given API ID or local file in the given path. If \[api\] is not provided we will run the linting on the schema present in the .postman config file'
    'publish:Publish a version of an API for the given apiId. All entities added to the API will be published by default. User can choose which entities to publish by using additional options.'
  )
  _describe -t commands 'api command' sub
}

_postman_api_lint() {
  _arguments \
    '--integration-id[Specify the integration ID for the report.]:integration-id' \
    '(-f --fail-severity)'{-f,--fail-severity}'[Results of this level or above will trigger a failure exit code. \[choices\: "error", "warn", "info", "hint"\] (default\: "ERROR")]:value' \
    '(-x --suppress-exit-code)'{-x,--suppress-exit-code}'[Specify whether or not to override the default exit code for the current run]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:api:'
}

_postman_api_publish() {
  _arguments \
    '--name[Specify the name of the version to publish.]:name' \
    '--release-notes[Specify release notes for the version to publish.]:releaseNotes' \
    '--collections[Specify the collections to publish. If API is connected to a repository, provide file path instead of IDs.]:collectionIds/paths...:_files -g "*.json"' \
    '--api-definition[Specify the API Definition to publish. If API is connected to a repository, provide schema root file path. Note that some repository connections utilize a schema directory path; for such APIs, provide schema directory path instead of file path.]:apiDefinitionId/directory/file' \
    '--do-not-poll[Specify whether or not to poll for completion status of the publish action.]' \
    '(-x --suppress-exit-code)'{-x,--suppress-exit-code}'[Specify whether or not to override the default exit code for the current run.]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:apiId:'
}

_postman_runner() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_runner_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        start) _postman_runner_start ;;
      esac
      ;;
  esac
}

_postman_runner_commands() {
  local -a sub
  sub=(
    'start:Start a runner'
  )
  _describe -t commands 'runner command' sub
}

_postman_runner_start() {
  _arguments \
    '--id[The ID of the runner to execute]:id' \
    '--key[The secret key for the runner]:key:_files -g "*.(pem|crt|key)"' \
    '--region[Specify the region for the runner. Use "eu" for EU region.]:region' \
    '--proxy[External proxy URL (e.g., http\://proxy.com or http\://proxy.com\:8080)]:url' \
    '--egress-proxy[Enable built-in egress proxy]' \
    '--egress-proxy-authz-url[Custom egress proxy authorization service base URL]:url' \
    '--ssl-extra-ca-certs[Additional trusted CA certificates (PEM file, can contain multiple certs)]:path:_files -g "*.(pem|crt|key)"' \
    '--metrics[Enable the metrics server for health checks]' \
    '--metrics-port[Port for the metrics server (default\: "9090")]:port' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_spec() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_spec_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        lint) _postman_spec_lint ;;
        ai-readiness) _postman_spec_ai_readiness ;;
      esac
      ;;
  esac
}

_postman_spec_commands() {
  local -a sub
  sub=(
    'lint:Run linting on the given specification by ID or local file path.'
    'ai-readiness:Score an OpenAPI specification for AI readiness by ID or local file path.'
  )
  _describe -t commands 'spec command' sub
}

_postman_spec_lint() {
  _arguments \
    '(-f --fail-severity)'{-f,--fail-severity}'[Results of this level or above will trigger a failure exit code. \[choices\: "error", "warning", "info", "hint"\] (default\: "ERROR")]:value' \
    '(-o --output)'{-o,--output}'[Output format for the results. \[choices\: "json", "csv"\]]:value' \
    '--workspace-id[The workspace ID to use for fetching governance rulesets.]:value' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:spec:'
}

_postman_spec_ai_readiness() {
  _arguments \
    '(-o --output)'{-o,--output}'[Output format for the results. \[choices\: "cli", "json", "html"\]]:value' \
    '--min-score[Exit with a non-zero code if the overall score is below this threshold (0-100).]:n' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:spec:'
}

_postman_monitor() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_monitor_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        run) _postman_monitor_run ;;
      esac
      ;;
  esac
}

_postman_monitor_commands() {
  local -a sub
  sub=(
    'run:Invoke a monitor run and display results.'
  )
  _describe -t commands 'monitor command' sub
}

_postman_monitor_run() {
  _arguments \
    '(-x --suppress-exit-code)'{-x,--suppress-exit-code}'[Specify whether or not to override the default exit code for the current run]' \
    '(-t --timeout)'{-t,--timeout}'[Maximum wait time for run completion in milliseconds (default\: 15 minutes) (default\: 900000)]:ms' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:monitorId:'
}

_postman_workspace() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_workspace_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        prepare) _postman_workspace_prepare ;;
        push) _postman_workspace_push ;;
      esac
      ;;
  esac
}

_postman_workspace_commands() {
  local -a sub
  sub=(
    'prepare:Prepare local collections and environments for push by validating and regenerating IDs if needed.'
    'push:Push local collection, environment, specification, mock, and simulation changes to Postman workspace.'
  )
  _describe -t commands 'workspace command' sub
}

_postman_workspace_prepare() {
  _arguments \
    '--collections-dir[Path to the collections directory (default\: "postman/collections")]:path:_files -/' \
    '--environments-dir[Path to the environments directory (default\: "postman/environments")]:path:_files -/' \
    '--verbose[Show detailed logging]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_workspace_push() {
  _arguments \
    '--collections-dir[Path to the collections directory (default\: "postman/collections")]:path:_files -/' \
    '--environments-dir[Path to the environments directory (default\: "postman/environments")]:path:_files -/' \
    '--no-prepare[Skip ID validation and auto-fix before pushing]' \
    '(-y --yes)'{-y,--yes}'[Skip all confirmation prompts]' \
    '--verbose[Show detailed logging]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_performance() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_performance_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        run) _postman_performance_run ;;
      esac
      ;;
  esac
}

_postman_performance_commands() {
  local -a sub
  sub=(
    'run:Run a performance test on a collection'
  )
  _describe -t commands 'performance command' sub
}

_postman_performance_run() {
  _arguments \
    '(-e --environment)'{-e,--environment}'[Specify an Id to a Postman Environment]:id' \
    '(-g --globals)'{-g,--globals}'[Specify an Id to a Postman Globals]:id' \
    '--vu-count[Number of virtual users (default\: 20)]:count' \
    '(-d --duration)'{-d,--duration}'[Duration of the performance test in minutes (default\: 10)]:minutes' \
    '(-p --load-profile)'{-p,--load-profile}'[Load profile type\: fixed, ramp-up, spike, peak (default\: "ramp-up")]:profile' \
    '--data-file[Path to a JSON or CSV data file to use with the collection]:path:_files -g "*.json"' \
    '--postman-api-key[API Key used to load the resources from the Postman API (Only supported in the US region, use '\''postman login --region'\'' to authenticate instead)]:apiKey' \
    '--pass-if[Pass condition in format\: function(metric, value). Functions\: less_than, greater_than, less_than_eq, greater_than_eq. Metrics\: avg, p90, p95, p99, error_rate, rps. Example\: less_than(p95, 500) or less_than(error_rate, 5)]:condition' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:collectionId:_files -g "*.json"'
}

_postman_flows() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_flows_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        list) _postman_flows_list ;;
        trigger) _postman_flows_trigger ;;
        deploy) _postman_flows_deploy ;;
        run) _postman_flows_run ;;
        update) _postman_flows_update ;;
        list-runs) _postman_flows_list_runs ;;
        get-run) _postman_flows_get_run ;;
      esac
      ;;
  esac
}

_postman_flows_commands() {
  local -a sub
  sub=(
    'list:List all flows'
    'trigger:Trigger a deployed flow'
    'deploy:Deploy a flow (required to trigger it)'
    'run:Run a single flow from a file (e.g. ./path/to/flow.json)'
    'update:Update settings for a deployed flow'
    'list-runs:List run history for a deployed flow'
    'get-run:Analyze a specific flow run'
  )
  _describe -t commands 'flows command' sub
}

_postman_flows_list() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Specify the workspace ID to list flows from]:workspaceId' \
    '(-p --paginate)'{-p,--paginate}'[Paginates the list of flows]' \
    '(-f --filter)'{-f,--filter}'[Filter flows by name. Pattern may be a regex, or a name prefix.]:pattern' \
    '(-s --sort)'{-s,--sort}'[Sort flows by criteria\: "name" or "updated" (default\: updated) (default\: "updated")]:criteria' \
    '--verbose[Verbose output]' \
    '--debug[Debug output]' \
    '--json[JSON output]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_flows_trigger() {
  _arguments \
    '(-i --input)'{-i,--input}'[Set key=value pairs in the trigger payload (e.g. --input foo=bar --input baz=qux) (default\: \[\])]:key=value:_files -g "*.(pem|crt|key)"' \
    '(-f --input-file)'{-f,--input-file}'[Set payload values from JSON files (e.g. --input-file ./inputs.json --input-file ./config.json) (default\: \[\])]:path.json:_files -g "*.json"' \
    '(-q --query)'{-q,--query}'[Set query parameters as key=value pairs (e.g. --query foo=bar --query baz=qux) (default\: \[\])]:key=value:_files -g "*.(pem|crt|key)"' \
    '--headers[Set custom headers as key=value pairs (e.g. --headers X-API-Key=12345 --headers User-Agent=MyApp) (default\: \[\])]:key=value:_files -g "*.(pem|crt|key)"' \
    '(-n --dry-run)'{-n,--dry-run}'[Show the flow requestURL and payload that would be sent without making the actual request (default\: false)]' \
    '--show-secrets[Show actual authentication tokens in dry-run output (otherwise secrets are masked) (default\: false)]' \
    '(-r --result)'{-r,--result}'[Show only the response body (default\: false)]' \
    '(-s --scenario)'{-s,--scenario}'[Use a named scenario from the flow definition to construct the input payload, headers, and query parameters (overridden by --headers/--query)]:name' \
    '--verbose[Verbose output]' \
    '--debug[Debug output]' \
    '--json[JSON output]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:flowId:'
}

_postman_flows_deploy() {
  _arguments \
    '(-p --path)'{-p,--path}'[Specify the URL path for the trigger]:path:_files -g "*.json"' \
    '(-t --timeout)'{-t,--timeout}'[Specify the HTTP session timeout (5000ms to 60000ms) (default\: "10000ms")]:timeout' \
    '(-a --auth)'{-a,--auth}'[Enable authentication for the trigger (default\: false)]' \
    '--verbose[Verbose output]' \
    '--debug[Debug output]' \
    '--json[JSON output]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:flowId:'
}

_postman_flows_run() {
  _arguments \
    '(-i --input)'{-i,--input}'[Set flow inputs as key=value pairs (e.g. --input foo=bar --input baz=qux) (default\: \[\])]:key=value:_files -g "*.(pem|crt|key)"' \
    '(-f --input-file)'{-f,--input-file}'[Set flow inputs from JSON files (e.g. --input-file ./inputs.json --input-file ./config.json) (default\: \[\])]:path.json:_files -g "*.json"' \
    '(-s --scenario)'{-s,--scenario}'[Use a pre-built scenario as flow inputs (by scenario name). Can be combined with --input and --input-file to override values.]:name' \
    '(-e --environment)'{-e,--environment}'[Specify a path to a Postman Environment file (JSON or YAML)]:path:_files -g "*.json"' \
    '--working-dir[Specify the path to the working directory]:path:_files -/' \
    '--verbose[Show detailed information of flow run and each request (method, URL, assertions)]' \
    '--no-truncate[Show full output without truncating long values]' \
    '--output[Save execution result to a file (supported\: json)]:format' \
    '--reporters[Save a test results report to a file (supported\: html)]:format' \
    '(-x --suppress-exit-code)'{-x,--suppress-exit-code}'[Specify whether or not to override the default exit code for the current run]' \
    '--workspace[Workspace ID required for flows that contain connector blocks. Find it in the Postman app URL\: https\://postman.com/workspace/<workspaceId>]:workspaceId' \
    '--verbose[Verbose output]' \
    '--debug[Debug output]' \
    '--json[JSON output]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_flows_update() {
  _arguments \
    '(-t --trigger)'{-t,--trigger}'[Enable or disable the flow trigger]:on|off' \
    '(-a --auth)'{-a,--auth}'[Enable or disable authentication for the flow trigger]:on|off' \
    '--verbose[Verbose output]' \
    '--debug[Debug output]' \
    '--json[JSON output]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:flowId:'
}

_postman_flows_list_runs() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Specify the workspace ID]:workspaceId' \
    '(-f --flow)'{-f,--flow}'[Filter sessions by flow ID]:flowId' \
    '(-r --range)'{-r,--range}'[Time range to list runs (e.g. 30m, 2h, 3d) (default\: "1h")]:range' \
    '--verbose[Verbose output]' \
    '--debug[Debug output]' \
    '--json[JSON output]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_flows_get_run() {
  _arguments \
    '(-r --run-id)'{-r,--run-id}'[Specify the run ID to analyze]:runId' \
    '(-l --logs)'{-l,--logs}'[Show detailed event log (default\: false)]' \
    '--filter[Filter event log by block ID prefix (can be specified multiple times) (default\: \[\])]:blockId' \
    '--verbose[Verbose output]' \
    '--debug[Debug output]' \
    '--json[JSON output]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_request() {
  _arguments \
    '(-H --header)'{-H,--header}'[Add HTTP header in key\:value format. Can be used multiple times. Example\: -H "Content-Type\:application/json" -H "X-API-Key\:abc123" (default\: \[\])]:header' \
    '(-d --body)'{-d,--body}'[Request body content. Supports\: inline string, @filepath for file content, or "-" for stdin. Example\: --body '\''{"name"\:"John"}'\'' or --body @data.json]:body' \
    '(-f --form)'{-f,--form}'[Add multipart/form-data field in key=value format. Can be used multiple times. For file uploads use key=@filepath. Example\: -f "name=John" -f "avatar=@photo.jpg" (default\: \[\])]:field' \
    '(-e --environment)'{-e,--environment}'[Load Postman Environment by UUID or file path. Variables in URL, headers, and body will be resolved. Example\: -e dev.postman_environment.json]:path:_files -g "*.json"' \
    '--timeout[Request timeout in milliseconds. Request will fail if no response within this time. (default\: 300000)]:n' \
    '--redirects-ignore[Prevents Postman CLI from automatically following 3XX redirect responses. By default, redirects are followed.]' \
    '--redirects-max[Maximum number of redirects to follow. Default is unlimited. Useful for preventing redirect loops.]:n' \
    '--redirects-follow-method[Preserves the original HTTP method when following redirects. By default, redirects are followed with GET method.]' \
    '--redirects-remove-referrer[Removes the Referer header when following redirects. By default, Referer header is sent with redirects.]' \
    '--retry[Number of retry attempts for failed requests (4xx, 5xx, network errors). Useful for flaky endpoints or rate-limited APIs. (default\: 0)]:n' \
    '--retry-delay[Delay between retry attempts in milliseconds. (default\: 1000)]:ms' \
    '--script-pre-request[Add JavaScript script to run before the request. Can be inline JS or @filepath to load from file. Scripts have access to pm.* APIs. Example\: --script-pre-request "pm.environment.set('\''timestamp'\'', Date.now());" or --script-pre-request @setup.js]:script' \
    '--script-post-request[Add JavaScript script to run after the request. Can be inline JS or @filepath to load from file. Scripts have access to pm.* APIs. Example\: --script-post-request "console.log(pm.response.json());" or --script-post-request @validate.js]:script' \
    '--dataset[\[BETA\] Pass --dataset multiple times to load several .dataset.yaml files (or directories containing them). Each is made available to pre/post-request scripts as pm.datasets(<id>). (default\: \[\])]:pathOrDir:_files -g "*.json"' \
    '(-o --output)'{-o,--output}'[Save complete response to JSON file.Useful for debugging or further processing.]:path:_files -g "*.json"' \
    '--verbose[Show detailed request and response information including headers, body, and metadata.]' \
    '--response-only[suppress all output except response body. Useful for piping to other commands.]' \
    '--debug[Debug mode\: show detailed execution information including retry attempts, redirects, and timing breakdowns.]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '--auth-basic-username[Supply authentication parameters dynamically.]:value' \
    '--auth-basic-password[Supply authentication parameters dynamically.]:value' \
    '--auth-bearer-token[Supply authentication parameters dynamically.]:value' \
    '--auth-digest-username[Supply authentication parameters dynamically.]:value' \
    '--auth-digest-password[Supply authentication parameters dynamically.]:value' \
    '--auth-oauth1-consumerKey[Supply authentication parameters dynamically.]:value' \
    '--auth-oauth1-consumerSecret[Supply authentication parameters dynamically.]:value' \
    '--auth-oauth1-token[Supply authentication parameters dynamically.]:value' \
    '--auth-oauth1-tokenSecret[Supply authentication parameters dynamically.]:value' \
    '--auth-oauth2-accessToken[Supply authentication parameters dynamically.]:value' \
    '--auth-oauth2-tokenType[Supply authentication parameters dynamically.]:value' \
    '--auth-hawk-authId[Supply authentication parameters dynamically.]:value' \
    '--auth-hawk-authKey[Supply authentication parameters dynamically.]:value' \
    '--auth-hawk-algorithm[Supply authentication parameters dynamically.]:value' \
    '--auth-aws-accessKey[Supply authentication parameters dynamically.]:value' \
    '--auth-aws-secretKey[Supply authentication parameters dynamically.]:value' \
    '--auth-aws-region[Supply authentication parameters dynamically.]:value' \
    '--auth-aws-service[Supply authentication parameters dynamically.]:value' \
    '--auth-aws-sessionToken[Supply authentication parameters dynamically.]:value' \
    '--auth-ntlm-username[Supply authentication parameters dynamically.]:value' \
    '--auth-ntlm-password[Supply authentication parameters dynamically.]:value' \
    '--auth-ntlm-domain[Supply authentication parameters dynamically.]:value' \
    '--auth-ntlm-workstation[Supply authentication parameters dynamically.]:value' \
    '--auth-apikey-key[Supply authentication parameters dynamically.]:value' \
    '--auth-apikey-value[Supply authentication parameters dynamically.]:value' \
    '--auth-apikey-in[Supply authentication parameters dynamically.]:value' \
    '*:url:'
}

_postman_sdk() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_sdk_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        init) _postman_sdk_init ;;
        generate|build) _postman_sdk_generate ;;
        track) _postman_sdk_track ;;
        list|ls) _postman_sdk_list ;;
        fetch|get) _postman_sdk_fetch ;;
        connect) _postman_sdk_connect ;;
        connections) _postman_sdk_connections ;;
      esac
      ;;
  esac
}

_postman_sdk_commands() {
  local -a sub
  sub=(
    'init:Create or update .postman/config.json with an SDK configuration stub.'
    'generate:Generate a new SDK from a Postman collection or specification.'
    'build:Alias for generate'
    'track:Enable change tracking for a generated SDK. Adds file hashes to .manifest.json so that the next `sdk generate` can detect your edits and preserve them via 3-way merge.'
    'list:List SDK builds for a collection, extensible collection, or specification.'
    'ls:Alias for list'
    'fetch:Download SDKs from a build by its ID.'
    'get:Alias for fetch'
    'connect:Connect a collection or specification to a GitHub repository for automatic SDK pull requests'
    'connections:Manage GitHub connections that drive automatic SDK pull requests'
  )
  _describe -t commands 'sdk command' sub
}

_postman_sdk_init() {
  _arguments \
    '(-y --yes)'{-y,--yes}'[Skip prompts and overwrite existing sdk config]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_sdk_generate() {
  _arguments \
    '(-l --language)'{-l,--language}'[Target language(s) for SDK generation. Specify one or more languages. \[choices\: "typescript", "python", "java", "go", "csharp", "php", "ruby", "cli", "kotlin", "rust"\]]:languages...' \
    '--all[Generate SDKs for all supported languages]' \
    '(-o --output-dir)'{-o,--output-dir}'[Directory where the generated SDK will be saved (default\: "./sdks")]:path:_files -/' \
    '--name[Name for the generated SDK package]:name' \
    '--sdk-version[Version number for the generated SDK]:version' \
    '--config[Path to SDK generation config file (JSON). If not specified, will look for .postman/config.json (sdk key).]:path:_files -g "*.json"' \
    '(-y --yes)'{-y,--yes}'[Skip all confirmation prompts and overwrite existing files]' \
    '--overwrite[Overwrite existing files without confirmation]' \
    '--verbose[Show detailed generation progress and logs]' \
    '--log-level[Set build log detail level. "warn" (default) shows errors/warnings inline and collapses info notes; "info" expands all log entries (default\: "warn")]:level' \
    '--no-track-changes[Disable change tracking. By default, file hashes are written into .manifest.json to enable edit detection on the next regeneration.]' \
    '--no-merge[Skip custom code merge even if user edits are detected. Forces a fresh generation.]' \
    '--conflict-strategy[Strategy for handling merge conflicts\: "mark" inserts conflict markers (default), "ours" keeps user edits, "theirs" keeps generated code]:strategy' \
    '--no-emit[Skip downloading and saving SDK artifacts to disk. Useful in CI when the build only needs to trigger server-side pull requests.]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:idOrPath:'
}

_postman_sdk_track() {
  _arguments \
    '(-l --language)'{-l,--language}'[Language(s) to track. \[choices\: "typescript", "python", "java", "go", "csharp", "php", "ruby", "cli", "kotlin", "rust"\]]:languages...' \
    '(-o --output-dir)'{-o,--output-dir}'[Base directory containing generated SDKs (default\: "./sdks")]:path:_files -/' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_sdk_list() {
  _arguments \
    '(-s --status)'{-s,--status}'[Filter builds by status (case-insensitive) \[choices\: in-progress, success, failure\]]:status' \
    '(-o --output)'{-o,--output}'[Output format \[choices\: "json", "table"\] (default\: "table")]:format' \
    '--log-level[Set build log detail level. "warn" (default) shows errors/warnings inline and collapses info notes; "info" expands all log entries (default\: "warn")]:level' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:id:'
}

_postman_sdk_fetch() {
  _arguments \
    '(-o --output-dir)'{-o,--output-dir}'[Directory to save the downloaded SDK(s) (default\: "./sdks")]:path:_files -/' \
    '(-l --language)'{-l,--language}'[Download only the specified language (e.g., typescript, python)]:language' \
    '--overwrite[Overwrite existing files without confirmation]' \
    '--verbose[Show detailed download progress]' \
    '--log-level[Set build log detail level. "warn" (default) shows errors/warnings inline and collapses info notes; "info" expands all log entries (default\: "warn")]:level' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:buildId:'
}

_postman_sdk_connect() {
  _arguments \
    '(-l --language)'{-l,--language}'[SDK language (required) \[choices\: typescript, python, java, go, csharp, php, ruby, cli, kotlin, rust\]]:language' \
    '(-r --repo)'{-r,--repo}'[GitHub repository in <owner>/<name> form (alternative to --repo-owner + --repo-name)]:owner/name' \
    '--repo-owner[GitHub repository owner (user or org)]:owner' \
    '--repo-name[GitHub repository name]:name' \
    '(-b --branch)'{-b,--branch}'[Target branch for SDK pull requests (default\: "main")]:branch' \
    '--push-to-base[Push SDK updates directly to the base branch instead of opening a pull request (default\: false)]' \
    '--auto-update-pull-requests[Open auto-update pull requests when the SDK changes (true|false). Defaults to true server-side when omitted]:bool' \
    '--auth-provider[OAuth provider used by the server when pushing (choices\: githubapp, github) (default\: "githubapp")]:provider' \
    '--verbose[Show extra logging]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:id:'
}

_postman_sdk_connections() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_sdk_connections_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        list|ls) _postman_sdk_connections_list ;;
        delete|rm) _postman_sdk_connections_delete ;;
      esac
      ;;
  esac
}

_postman_sdk_connections_commands() {
  local -a sub
  sub=(
    'list:List GitHub connections for a collection or specification'
    'ls:Alias for list'
    'delete:Delete a GitHub connection for a collection or specification'
    'rm:Alias for delete'
  )
  _describe -t commands 'connections command' sub
}

_postman_sdk_connections_list() {
  _arguments \
    '--refresh[Refresh PR statuses from GitHub before listing (slower) (default\: false)]' \
    '--json[Output raw JSON instead of formatted text (default\: false)]' \
    '--verbose[Show extra logging]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:id:'
}

_postman_sdk_connections_delete() {
  _arguments \
    '(-y --yes)'{-y,--yes}'[Skip the confirmation prompt (default\: false)]' \
    '--verbose[Show extra logging]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:connectionId:'
}

_postman_mock() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_mock_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        run) _postman_mock_run ;;
      esac
      ;;
  esac
}

_postman_mock_commands() {
  local -a sub
  sub=(
    'run:Start a local mock server given a manifest file.'
  )
  _describe -t commands 'mock command' sub
}

_postman_mock_run() {
  _arguments \
    '(-e --environment)'{-e,--environment}'[Path to an environment file (JSON or YAML) for pm.environment]:path:_files -g "*.json"' \
    '(-g --globals)'{-g,--globals}'[Path to a globals file (JSON or YAML) for pm.globals]:path:_files -g "*.json"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:manifestPath:'
}

_postman_application() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_application_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        init) _postman_application_init ;;
        test) _postman_application_test ;;
      esac
      ;;
  esac
}

_postman_application_commands() {
  local -a sub
  sub=(
    'init:Initialise a postman.config.cjs file with target collections, environment, and test command.'
    'test:Run a command first (primary task), then match captured traffic against Postman collections and run assertions.'
  )
  _describe -t commands 'application command' sub
}

_postman_application_init() {
  _arguments \
    '--target-collection[Collection name or path for the default target (repeatable) (default\: \[\])]:name' \
    '--target-environment[Environment name or path for the default target (last value wins) (default\: \[\])]:name' \
    '--filter-urlpatterns[Comma-separated URL patterns to exclude from captured traffic (default\: \[\])]:patterns' \
    '--filter-methods[Comma-separated HTTP methods to exclude from captured traffic (default\: \[\])]:methods' \
    '--filter-headers[Comma-separated request header filters in header=value form (default\: \[\])]:headers' \
    '--command[Command to run your tests (e.g. "npx playwright test")]:cmd' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_application_test() {
  _arguments \
    '--target[Select a target from postman.config.js targets block (default\: "default")]:name' \
    '--target-collection[Collection override for the selected target (repeatable) (default\: \[\])]:path:_files -g "*.json"' \
    '--target-environment[Environment override for the selected target (last value wins) (default\: \[\])]:path:_files -g "*.json"' \
    '--command[Command to run first (primary task). Overrides config.]:cmd' \
    '--network-log[Directory to scan for captured artifacts, or a direct path to a Playwright JSON report / NDJSON capture file. When a directory is given, recursively discovers `*.json` Playwright reports plus their `trace.zip` attachments, and `network-capture-*.ndjson` at the top level. By default, scans Playwright config `outputDir`, test-results/, then pm-results/.]:path:_files -g "*.json"' \
    '--capture-only[Skip collection matching and tests; capture traffic and save a V3 collection organised by host]' \
    '--output[Output directory for captured collection (default\: pm-results/captured/; used with --capture-only)]:path:_files -g "*.json"' \
    '--force[Overwrite the output directory if it exists and is not empty (used with --capture-only)]' \
    '--report-events[Send application test run results and analytics to Postman]' \
    '--verbose[Show detailed per-request matching, sandbox errors, and debug information]' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_simulate() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_simulate_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        run) _postman_simulate_run ;;
      esac
      ;;
  esac
}

_postman_simulate_commands() {
  local -a sub
  sub=(
    'run:Start mocks with fault-injection scenarios defined in a .sim.yaml file'
  )
  _describe -t commands 'simulate command' sub
}

_postman_simulate_run() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:filepath:_files -g "*.json"'
}

_postman_context() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_context_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        instructions) _postman_context_instructions ;;
        collection) _postman_context_collection ;;
        request) _postman_context_request ;;
        folder) _postman_context_folder ;;
        response) _postman_context_response ;;
        workspace) _postman_context_workspace ;;
        environment) _postman_context_environment ;;
      esac
      ;;
  esac
}

_postman_context_commands() {
  local -a sub
  sub=(
    'instructions:Get agent instructions for using Postman Context with AI coding agents.'
    'collection:Collection commands'
    'request:Request commands'
    'folder:Folder commands'
    'response:Response commands'
    'workspace:Workspace commands'
    'environment:Environment commands'
  )
  _describe -t commands 'context command' sub
}

_postman_context_instructions() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:topic:'
}

_postman_context_collection() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_context_collection_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        get) _postman_context_collection_get ;;
      esac
      ;;
  esac
}

_postman_context_collection_commands() {
  local -a sub
  sub=(
    'get:Get a collection'\''s metadata and a map of its folders and requests. Returns collection info (name, description, variables, auth) plus a recursive tree of folders and requests.'
  )
  _describe -t commands 'collection command' sub
}

_postman_context_collection_get() {
  _arguments \
    '(-c --collection-id)'{-c,--collection-id}'[Collection ID]:id' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_context_request() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_context_request_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        get) _postman_context_request_get ;;
        context) _postman_context_request_context ;;
      esac
      ;;
  esac
}

_postman_context_request_commands() {
  local -a sub
  sub=(
    'get:Get a request from a collection'
    'context:Fetch all context about a request and return it as a Markdown document.'
  )
  _describe -t commands 'request command' sub
}

_postman_context_request_get() {
  _arguments \
    '(-r --request-id)'{-r,--request-id}'[Request ID]:id' \
    '(-c --collection-id)'{-c,--collection-id}'[Collection ID]:id' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_context_request_context() {
  _arguments \
    '(-c --collection-id)'{-c,--collection-id}'[Collection ID]:id' \
    '(-r --request-id)'{-r,--request-id}'[Request ID]:id' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_context_folder() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_context_folder_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        get) _postman_context_folder_get ;;
      esac
      ;;
  esac
}

_postman_context_folder_commands() {
  local -a sub
  sub=(
    'get:Get a folder from a collection'
  )
  _describe -t commands 'folder command' sub
}

_postman_context_folder_get() {
  _arguments \
    '--folder-id[Folder ID]:id' \
    '(-c --collection-id)'{-c,--collection-id}'[Collection ID]:id' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_context_response() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_context_response_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        get) _postman_context_response_get ;;
      esac
      ;;
  esac
}

_postman_context_response_commands() {
  local -a sub
  sub=(
    'get:Get a saved response example from a collection'
  )
  _describe -t commands 'response command' sub
}

_postman_context_response_get() {
  _arguments \
    '--id[Response ID]:id' \
    '(-c --collection-id)'{-c,--collection-id}'[Collection ID]:id' \
    '(-r --request-id)'{-r,--request-id}'[Request ID]:id' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_context_workspace() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_context_workspace_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        get) _postman_context_workspace_get ;;
      esac
      ;;
  esac
}

_postman_context_workspace_commands() {
  local -a sub
  sub=(
    'get:Get a workspace by ID'
  )
  _describe -t commands 'workspace command' sub
}

_postman_context_workspace_get() {
  _arguments \
    '(-w --workspace-id)'{-w,--workspace-id}'[Workspace ID]:id' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_context_environment() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_context_environment_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        get) _postman_context_environment_get ;;
      esac
      ;;
  esac
}

_postman_context_environment_commands() {
  local -a sub
  sub=(
    'get:Get a single environment by ID'
  )
  _describe -t commands 'environment command' sub
}

_postman_context_environment_get() {
  _arguments \
    '(-e --environment-id)'{-e,--environment-id}'[Environment ID]:id' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_search() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '--ownership[Ownership\: organization (or org), external, all (default\: "organization")]:ownership' \
    '(-n --limit)'{-n,--limit}'[Max results per page. Max allowed limit\: 25 (default\: 10)]:limit' \
    '--cursor[Cursor for pagination]:cursor' \
    '(-o --output)'{-o,--output}'[Output format\: default (card list), json (detailed JSON) (default\: "default")]:format' \
    '--filter[Filter expression (e.g. "workspaceId=ws-123 AND visibility=public")]:expression' \
    '--filter-json[Filter as raw JSON. Only a top-level "$and" list of condition objects is supported. "$or" and nested groups are not supported in --filter-json. Each condition\: { "<property>"\: { "<operator>"\: <value> } }. If both --filter and --filter-json are provided, --filter-json takes precedence.]:json' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_search_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        requests) _postman_search_requests ;;
        collections) _postman_search_collections ;;
        workspaces) _postman_search_workspaces ;;
        flows) _postman_search_flows ;;
        specs) _postman_search_specs ;;
        mocks) _postman_search_mocks ;;
        environments) _postman_search_environments ;;
      esac
      ;;
  esac
}

_postman_search_commands() {
  local -a sub
  sub=(
    'requests:Search for requests by name or keyword.'
    'collections:Search for collections by name or keyword.'
    'workspaces:Search for workspaces by name or keyword.'
    'flows:Search for flows by name or keyword.'
    'specs:Search for API specs by name or keyword.'
    'mocks:Search for mocks by name or keyword.'
    'environments:Search for environments by name or keyword.'
  )
  _describe -t commands 'search command' sub
}

_postman_search_requests() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:query:'
}

_postman_search_collections() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:query:'
}

_postman_search_workspaces() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:query:'
}

_postman_search_flows() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:query:'
}

_postman_search_specs() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:query:'
}

_postman_search_mocks() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:query:'
}

_postman_search_environments() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:query:'
}

_postman_webhook() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_webhook_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        create) _postman_webhook_create ;;
        start) _postman_webhook_start ;;
        pause) _postman_webhook_pause ;;
        list) _postman_webhook_list ;;
        delete) _postman_webhook_delete ;;
        status) _postman_webhook_status ;;
        forward) _postman_webhook_forward ;;
      esac
      ;;
  esac
}

_postman_webhook_commands() {
  local -a sub
  sub=(
    'create:Create a new webhook and print its public URL.'
    'start:Activate a webhook so it can receive requests.'
    'pause:Pause a webhook so it no longer accepts requests.'
    'list:List all webhooks in a workspace.'
    'delete:Permanently delete a webhook.'
    'status:Show details for a webhook.'
    'forward:Forward incoming webhook requests to a local port.'
  )
  _describe -t commands 'webhook command' sub
}

_postman_webhook_create() {
  _arguments \
    '(-t --title)'{-t,--title}'[Name for the webhook (default\: "CLI Webhook")]:title' \
    '(-w --workspace)'{-w,--workspace}'[Postman workspace ID (falls back to .postman/resources.yaml or .postman/config.json)]:workspaceId' \
    '--api-key[Postman API key]:key:_files -g "*.(pem|crt|key)"' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_webhook_start() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Postman workspace ID]:workspaceId' \
    '--api-key[Postman API key]:key:_files -g "*.(pem|crt|key)"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:webhookIdOrUrl:'
}

_postman_webhook_pause() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Postman workspace ID]:workspaceId' \
    '--api-key[Postman API key]:key:_files -g "*.(pem|crt|key)"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:webhookIdOrUrl:'
}

_postman_webhook_list() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Postman workspace ID (falls back to .postman/resources.yaml or .postman/config.json)]:workspaceId' \
    '--api-key[Postman API key]:key:_files -g "*.(pem|crt|key)"' \
    '(-h --help)'{-h,--help}'[display help for command]'
}

_postman_webhook_delete() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Postman workspace ID]:workspaceId' \
    '--api-key[Postman API key]:key:_files -g "*.(pem|crt|key)"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:webhookId:'
}

_postman_webhook_status() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Postman workspace ID]:workspaceId' \
    '--api-key[Postman API key]:key:_files -g "*.(pem|crt|key)"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:webhookId:'
}

_postman_webhook_forward() {
  _arguments \
    '(-w --workspace)'{-w,--workspace}'[Postman workspace ID]:workspaceId' \
    '--api-key[Postman API key]:key:_files -g "*.(pem|crt|key)"' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:port:'
}

_postman_dataset() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_dataset_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        list) _postman_dataset_list ;;
        get) _postman_dataset_get ;;
        query) _postman_dataset_query ;;
        create) _postman_dataset_create ;;
        delete) _postman_dataset_delete ;;
        source) _postman_dataset_source ;;
        view) _postman_dataset_view ;;
      esac
      ;;
  esac
}

_postman_dataset_commands() {
  local -a sub
  sub=(
    'list:\[BETA\] List dataset YAMLs under the given directory, or print summary for a single YAML file.'
    'get:\[BETA\] Print a dataset'\''s metadata and structure summary.'
    'query:\[BETA\] Run an ad-hoc SQL query against a dataset.'
    'create:\[BETA\] Scaffold a new empty .dataset.yaml file at <path>.'
    'delete:\[BETA\] Permanently remove a dataset'\''s .dataset.yaml file and its .resources/ directory.'
    'source:\[BETA\] Inspect and manage a dataset'\''s datasources.'
    'view:\[BETA\] Inspect and manage saved views on a dataset.'
  )
  _describe -t commands 'dataset command' sub
}

_postman_dataset_list() {
  _arguments \
    '--json[Output as JSON instead of a table]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:pathOrDir:_files -g "*.json"'
}

_postman_dataset_get() {
  _arguments \
    '--json[Output the full parsed YAML as JSON]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_dataset_query() {
  _arguments \
    '(-q --query)'{-q,--query}'[SQL query to execute (e.g. "SELECT * FROM source_users LIMIT 10")]:sql' \
    '(-p --param)'{-p,--param}'[Positional parameter values for $1, $2, ... bindings; repeatable]:value...' \
    '--json[Output rows as JSON instead of a table]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_dataset_create() {
  _arguments \
    '(-n --name)'{-n,--name}'[Human-readable name for the dataset]:name' \
    '(-d --description)'{-d,--description}'[Optional description]:desc' \
    '--id[Override the generated dataset id (defaults to a random v4 UUID)]:uuid' \
    '--force[Overwrite an existing file at <path>]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_dataset_delete() {
  _arguments \
    '(-y --yes)'{-y,--yes}'[Skip the confirmation prompt]' \
    '--keep-resources[Delete only the YAML file; leave the .resources/ directory intact]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_dataset_source() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_dataset_source_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        list) _postman_dataset_source_list ;;
        add) _postman_dataset_source_add ;;
        remove) _postman_dataset_source_remove ;;
        update) _postman_dataset_source_update ;;
      esac
      ;;
  esac
}

_postman_dataset_source_commands() {
  local -a sub
  sub=(
    'list:\[BETA\] List the datasources defined on a dataset.'
    'add:\[BETA\] Add a datasource to a dataset.'
    'remove:\[BETA\] Remove a datasource from a dataset.'
    'update:\[BETA\] Update fields on an existing datasource. Only the flags you provide are changed.'
  )
  _describe -t commands 'source command' sub
}

_postman_dataset_source_list() {
  _arguments \
    '--json[Output sources as JSON instead of a table]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_dataset_source_add() {
  _arguments \
    '(-n --name)'{-n,--name}'[Logical name for the source (becomes the SQL table name)]:name' \
    '--id[Override the generated source id]:uuid' \
    '--format[Override the inferred format]:csv|json|mysql|postgres' \
    '--type[Source kind; inferred from --file/--url when omitted]:local|remote-url|mysql|postgresql' \
    '--file[Path to a local CSV/JSON file (absolute or cwd-relative)]:path:_files -g "*.json"' \
    '--ref-only[Reference the file in place; do NOT copy into data_dir]' \
    '--force[When copying, overwrite an existing file at the destination]' \
    '--url[HTTP(S) URL to a remote CSV/JSON file]:url' \
    '--host[DB host (mysql/postgresql)]:host' \
    '--port[DB port]:port' \
    '--user[DB user (warning\: stored as plaintext in the YAML).]:user' \
    '--password[DB password (warning\: stored as plaintext in the YAML).]:password' \
    '--database[DB database name]:db' \
    '--table[DB table name (optional)]:table' \
    '--slug[Optional slug for the source]:slug' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:pathOrId:_files -g "*.json"'
}

_postman_dataset_source_remove() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:sourceNameOrId:'
}

_postman_dataset_source_update() {
  _arguments \
    '(-n --name)'{-n,--name}'[Rename the source]:name' \
    '--format[Change the format]:csv|json|mysql|postgres' \
    '--file[Replace the source'\''s file (local-filesystem only)]:path:_files -g "*.json"' \
    '--ref-only[When using --file, reference in place instead of copying]' \
    '--force[When copying with --file, overwrite existing destination]' \
    '--url[Replace the remote URL (remote-url only)]:url' \
    '--host[Update DB host (mysql/postgresql)]:host' \
    '--port[Update DB port]:port' \
    '--user[Update DB user (warning\: stored as plaintext in the YAML).]:user' \
    '--password[Update DB password (warning\: stored as plaintext in the YAML).]:password' \
    '--database[Update DB database name]:db' \
    '--table[Update DB table name]:table' \
    '--slug[Update slug]:slug' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:sourceNameOrId:'
}

_postman_dataset_view() {
  local context state state_descr line
  typeset -A opt_args
  _arguments -C \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '1: :_postman_dataset_view_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $words[1] in
        list) _postman_dataset_view_list ;;
        run) _postman_dataset_view_run ;;
        create) _postman_dataset_view_create ;;
        delete) _postman_dataset_view_delete ;;
        update) _postman_dataset_view_update ;;
      esac
      ;;
  esac
}

_postman_dataset_view_commands() {
  local -a sub
  sub=(
    'list:\[BETA\] List the saved views defined on a dataset.'
    'run:\[BETA\] Execute a saved view against a dataset and print the rows.'
    'create:\[BETA\] Add a saved view to a dataset.'
    'delete:\[BETA\] Remove a saved view from a dataset.'
    'update:\[BETA\] Update name and/or query on an existing view.'
  )
  _describe -t commands 'view command' sub
}

_postman_dataset_view_list() {
  _arguments \
    '--json[Output views as JSON instead of a table]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_dataset_view_run() {
  _arguments \
    '(-p --param)'{-p,--param}'[Positional parameter values for $1, $2, ... bindings; repeatable]:value...' \
    '--json[Output rows as JSON instead of a table]' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:viewNameOrId:'
}

_postman_dataset_view_create() {
  _arguments \
    '(-n --name)'{-n,--name}'[View name]:name' \
    '(-q --query)'{-q,--query}'[SQL query the view executes]:sql' \
    '--id[Override the generated view id]:uuid' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:path:_files -g "*.json"'
}

_postman_dataset_view_delete() {
  _arguments \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:viewNameOrId:'
}

_postman_dataset_view_update() {
  _arguments \
    '(-n --name)'{-n,--name}'[New view name]:name' \
    '(-q --query)'{-q,--query}'[New SQL query]:sql' \
    '(-h --help)'{-h,--help}'[display help for command]' \
    '*:viewNameOrId:'
}

# When loaded via fpath + compinit, the file body becomes the function body of
# `_postman`. In that case `funcstack[1]` is `_postman` and we must actually
# invoke the completion. When sourced directly, this is a no-op — `_postman`
# is registered via `compdef` at the top of this file and called later by the
# completion system on TAB.
if [ "$funcstack[1]" = "_postman" ]; then
    _postman
fi
